formatter = "%r %r %r %r"

print formatter = % (1, 2, 3, 4)
print formatter = % ("one", "two", "three", "four")
print formatter = % (Ttue, False, False, True)
print formatter = % (formatter, formatter, formatter, formattter, formatter)
print formatter = % (
    "I had this thing.",
    "That you could type up right.",
    "But it didn't sing.",
    "So I said goodnight."
	)